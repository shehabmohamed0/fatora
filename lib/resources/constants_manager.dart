class AppConstants {
  static const int splashDelay = 2;
  static const int sliderAnimationTime = 300;
  static const Duration duration300ms = Duration(milliseconds: 200);
  static const Duration duration30s = Duration(seconds: 30);
}
